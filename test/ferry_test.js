var assert = require('assert');
var car = require('./car');

describe("The number of cars and people that can get onto a Ferry.", function(done) {
  it("Should specify the colour and the number of passengers in the car.", function(done){

    
    assert.equal(1,2);
    done;
  })
  done;
});
